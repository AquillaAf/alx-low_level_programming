#include <stdio.h>
#include <stdlib.h>
/**
 * main - function prints its name
 * @argc: length of command arguments
 * @argv: its string length
 *
 * Return: (0)
 */
int main(int argc, char *argv[])
{
	if (argc <= 1)
	{
		printf("%s\n", argv[0]);
	}
	else
	{
	}
return (0);
}
