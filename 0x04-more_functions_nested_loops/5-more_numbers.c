#include "main.h"
/**
 * more_numbers - function to multiply an array
 *
 */
void more_numbers(void)
{
	int k;

	for (k = 1; k <= 10; k++)
	{
		int i = 0;
		while (i <= 14)
		i++;
		_putchar (i);
	}
	_putchar ('\n');
}
