#include <stdio.h>
/**
 * a]main - Entry point
 * Description: 'print base 10 numbers'
 * Return: always 0
 */
int main(void)
{
	int k;

	for (k = 0; k <= 9; k++)
	{
		printf("%d", k);
	}
	printf("\n");
	return (0);
}
